# 計画テンプレート

`docs/ai/plans/<YYYY-MM-DD>-<slug>.md` としてコピーして使う。
**この計画ファイルが意図の全量になる。**実装エージェント（Codex）は会話履歴を
持たないため、ここに書いていないことは実装されない。

---

# <タイトル>

- **状態**: draft | codex-reviewed | approved | implemented
- **規模**: 軽 | 中 | 重
- **作成日**: YYYY-MM-DD
- **ベースライン**: `git status --short` / `git diff --stat` の実行結果を貼る

## 背景と問題

何が起きているか。観測された事実と、推測（未検証の仮説）を分けて書く。

## 原因（根拠つき）

ファイル名:行番号で示す。検証済みか未検証かを明記する。

## 対象範囲

### 変更してよいファイル

- `path/to/file.js` — 何をするか

### 変更禁止

- `path/to/other.js` — 理由

## 受入条件

**測定可能な形で書く。**「動くこと」ではなく「Xを入力するとYになること」まで
落とす。数値の閾値は事前登録し、後から動かさない。

### 機械的に検証できる条件

- [ ] AC1: ...

### 人間が実機で確認する条件（機械では閉じられない）

- [ ] HC1: ...

## 制約（守るべき既存設計）

過去の教訓・パリティ制約など、壊してはいけないもの。

## 書かなかったこと（スコープ外）

意図的に含めなかったもの。Codexが善意で手を広げるのを防ぐ。

## 失敗時の停止条件

「Xが通らなければYに進まず停止して報告する」の形で書く。

## テストコマンド

```bash
```

## 人間の判断が必要な選択肢

トレードオフが大きくCodexにも決めさせない事項。承認ゲートで選ぶ。
